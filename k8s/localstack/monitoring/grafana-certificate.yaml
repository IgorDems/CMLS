apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: monitoring
spec:
  secretName: grafana-tls-secret
  issuerRef:
    name: cloudmart-selfsigned
    kind: ClusterIssuer
  commonName: grafana.cloudmart.lab
  dnsNames:
    - grafana.cloudmart.lab
