apiVersion: v1
kind: Service
metadata:
  name: cloudmart-frontend-app-service
  namespace: cloudmart
spec:
  type: ClusterIP
  selector:
    app: cloudmart-frontend-app
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
