apiVersion: v1
kind: Service
metadata:
  name: cloudmart-frontend
  namespace: cloudmart
spec:
  selector:
    app: cloudmart-frontend
  ports:
    - protocol: TCP
      port: 3000       # Зовнішній порт сервісу
      targetPort: 3000 # ПОРТ У КОНТЕЙНЕРІ
