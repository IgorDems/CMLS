apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: cloudmart
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: open-webui
  type: ClusterIP # Внутрішній сервіс, доступ через Ingress
