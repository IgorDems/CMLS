apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: localstack-cert
  namespace: default
spec:
  secretName: localstack-tls
  issuerRef:
    name: local-ca
    kind: ClusterIssuer
  commonName: localstack.lab
  dnsNames:
    - localstack.lab
