apiVersion: v1
kind: Service

metadata:
  name: cloudmart-backend
  namespace: cloudmart

spec:
  type: ClusterIP
  selector:
    app: cloudmart-backend
  ports:
  - name: http
    port: 80
    targetPort: 5000
